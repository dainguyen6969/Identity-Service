server:
  port: 8080
  servlet:
    context-path: /identity

spring:
  datasource:
    url: ${DBMS_CONNECTION:jdbc:mysql://mysql-container:3306/Identity_Service?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
    driver-class-name: "com.mysql.cj.jdbc.Driver"
    username: ${DBMS_USERNAME:root}
    password: ${DBMS_PASSWORD:123456}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

jwt:
  signerKey: "iFmZsoXsVNPZfC+SqJ+B5esbt9PWchEqB8/Fn/qG26GPwvCY2m6loFrPETLRCeqh"
  valid-duration: 3600 # in second
  refreshable-duration: 360000 # in seconds